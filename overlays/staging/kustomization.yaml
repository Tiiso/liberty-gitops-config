apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: liberty-demo-staging

resources:
  - ../../base

labels:
  - pairs:
      environment: staging

images:
  - name: image-registry.openshift-image-registry.svc:5000/liberty-demo-prod/liberty-app
    newTag: staging
